`ifndef NOT
	`define NOT not
`endif
`ifndef XOR
	`define XOR xor
`endif
`ifndef AND
	`define AND and
`endif
`ifndef NAND
	`define NAND nand
`endif
`ifndef NOR
	`define NOR nor
`endif
`ifndef OR
	`define OR or
`endif